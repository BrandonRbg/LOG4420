extends layout

block content
  article
    h1
      | Votre commande est confirmée
      span#name  #{lastOrder.firstName + " " + lastOrder.lastName}
      | !
    p
      | Votre numéro de confirmation est le
      strong#confirmation-number  #{confirmationNumber}
      | .
